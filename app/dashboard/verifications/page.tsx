import { AdminVerificationsPageContent } from "@/features/dashboard/ui/admin/admin-verifications-page";

export const metadata = {
  title: "Verifications — Admin",
};

export default function AdminVerificationsPage() {
  return <AdminVerificationsPageContent />;
}
