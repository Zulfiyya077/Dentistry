import { AdminUsersPageContent } from "@/features/dashboard/ui/admin/admin-users-page";

export const metadata = {
  title: "Users — Admin",
};

export default function AdminUsersPage() {
  return <AdminUsersPageContent />;
}
